<h1>/list</h1>
<% if @list.any? %>
  <%= "<table class='table table-striped'>" %>
  <%= "<tr>
        <th>.item</th>
        <th>.qty</th>
        <th>.notes</th>
        <th>.added</th>
        <th>.destroy</th>
      </tr>" %>
  <% @list.each do |item| %>
  <%= "<tr>
        <td>#{item.item}</td>
        <td>#{item.quantity}</td>
        <td>#{item.note}</td>
        <td>#{item.added_on.strftime("%m/%e/%Y")}</td>
        <td><a href='/#{item.id}/list/destroy')'>x</a></td>
      </tr>" %>
  <% end %>
  <%= "</table>" %>
<% else %>
  <%= "nothing currently here!" %>  
<% end %>
<p class="text-right"><a href="/list/add">./add</a></p>
